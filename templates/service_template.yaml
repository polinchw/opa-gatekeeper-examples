# Demo use only
---
apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: servicelabels
spec:
  crd:
    spec:
      names:
        kind: ServiceLabels
        listKind: ServiceLabelsList
        plural: servicelabels
        singular: servicelabels
      validation:
        openAPIV3Schema:
          properties:
            annotations:
              type: array
              items: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
         package servicelabels

         violation[{"msg": msg}] {
           sprintf(input.review.object.metadata.annotations,["hi"])
           not input.review.object.metadata.annotations.loadbalancernetwork == "private"
           msg := "Error you must use a private network in the annotation"
         }

---
